app:
  name: ginny-template
http:
  mode: debug
  port: 8080
log:
  filename: ./logs/test.log
  maxSize: 500
  maxBackups: 3
  maxAge: 3
  level: "debug"
  stdout: false
consul:
  addr: ${consul_addr}
grpc:
  port: 0
jaeger:
  serviceName: products
  reporter:
    localAgentHostPort: "jaeger-agent:6831"
  sampler:
    type: const
    param: 1
